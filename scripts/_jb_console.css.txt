  /* ===== v0.30.x jb-console — DJ VINYL'S CONSOLE (per user: "WAY more hip popular and stylish with drift phonk vibes",
     "each BGM also has a unique icon", "like DJ console concept where each button on the DJ console plays a specific
     music", and on a pad-controller photo: "simple and cute, well mapped and organised"). A little black controller:
     two jog wheels with rainbow LED rings (the left one spins the record, the right one is the music volume), a rainbow
     fan meter in the middle, and one backlit pad per track, zoned by colour like the photo. ===== */
  #jukebox-modal-bg {
    position: fixed; inset: 0;
    /* the club: two stage beams (pink, cyan) sweeping down from the rig, haze low on the floor */
    background: conic-gradient(from 150deg at 14% -8%, transparent 0deg, rgba(255,79,163,0.20) 10deg, rgba(255,79,163,0.07) 22deg, transparent 30deg),
                conic-gradient(from 180deg at 86% -8%, transparent 0deg, rgba(62,232,255,0.07) 8deg, rgba(62,232,255,0.18) 20deg, transparent 30deg),
                radial-gradient(ellipse 80% 40% at 50% 105%, rgba(185,140,255,0.22) 0%, transparent 70%),
                rgba(5,3,11,0.88);
    display: none; align-items: center; justify-content: center;
    z-index: 9999;
  }
  #jukebox-modal-bg.on { display: flex; }
  /* the controller: a matte black faceplate with a fine brushed grain, a gunmetal bezel and four hex screws */
  #jukebox-modal-bg #jukebox-modal {
    --jbA: #3ee8ff; --jbB: #8dff5a; --jbC: #b98cff; --jbD: #ff4fa3; --jbY: #ffd84a;
    position: relative;
    width: min(980px, 96vw); max-height: 94vh;
    display: flex; flex-direction: column; overflow: hidden;
    color: #f4f0ff; font-family: "Segoe UI", system-ui, -apple-system, sans-serif;
    background-image: radial-gradient(90% 45% at 50% -8%, rgba(185,140,255,0.13), transparent 70%),
                      repeating-linear-gradient(90deg, rgba(255,255,255,0.022) 0 1px, rgba(0,0,0,0) 1px 3px),
                      linear-gradient(180deg, #1d1c23 0%, #131218 42%, #0b0a0f 100%) !important;
    background-size: auto !important; background-repeat: no-repeat, repeat, no-repeat !important;
    /* the bezel is a border and an outline, not a shadow: the low-graphics mode (html.lx-nobackdrop) strips every
       box-shadow in the game, and the console has to read as hardware there too. Shadows only add the glow. */
    box-sizing: border-box; border: 4px solid #25232d; border-top-color: #34313f; border-bottom-color: #17161c; border-radius: 26px;
    outline: 2px solid #050408; outline-offset: 0;
    box-shadow: 0 26px 60px rgba(0,0,0,0.75), 0 0 80px rgba(255,79,163,0.22), inset 0 1px 0 rgba(255,255,255,0.12);
  }
  #jukebox-modal-bg #jukebox-modal::before {
    content: ''; position: absolute; inset: 9px; pointer-events: none; z-index: 0;
    background: radial-gradient(circle at 7px 7px, #6d6a7a 0 1.5px, #2a2832 2.5px 5px, rgba(0,0,0,0) 5.5px) top left / 14px 14px no-repeat,
                radial-gradient(circle at 7px 7px, #6d6a7a 0 1.5px, #2a2832 2.5px 5px, rgba(0,0,0,0) 5.5px) top right / 14px 14px no-repeat,
                radial-gradient(circle at 7px 7px, #6d6a7a 0 1.5px, #2a2832 2.5px 5px, rgba(0,0,0,0) 5.5px) bottom left / 14px 14px no-repeat,
                radial-gradient(circle at 7px 7px, #6d6a7a 0 1.5px, #2a2832 2.5px 5px, rgba(0,0,0,0) 5.5px) bottom right / 14px 14px no-repeat;
  }
  #jukebox-modal > * { position: relative; z-index: 1; }
  /* silkscreen: the small printed labels on the faceplate */
  #jukebox-modal .jb-silk { display: block; text-align: center; font: 800 9px/1 "Segoe UI", system-ui, sans-serif; letter-spacing: 2.4px; color: #8f8aa3; text-transform: uppercase; }
  #jukebox-modal .jb-plate { flex: none; display: flex; flex-direction: column; }
  #jukebox-modal .jb-model { display: block; font: 700 9px/1 ui-monospace, Consolas, monospace; letter-spacing: 2px; color: #77728a; margin-top: 3px; }
  /* the RGB underglow along the bottom edge */
  #jukebox-modal-bg #jukebox-modal::after {
    content: ''; position: absolute; left: 7%; right: 7%; bottom: 0; height: 3px; border-radius: 3px; pointer-events: none;
    background: linear-gradient(90deg, var(--jbA), var(--jbB), var(--jbY), var(--jbD), var(--jbC));
    box-shadow: 0 0 14px 2px rgba(255,79,163,0.55);
  }
  /* ---- top bar: badge, name plate, katakana sticker, found counter, close ---- */
  #jukebox-modal .jb-top { display: flex; align-items: center; gap: 10px; padding: 12px 16px 4px 18px; }
  #jukebox-modal .jb-badge { width: 46px; height: 46px; object-fit: contain; flex: none; filter: drop-shadow(0 0 8px rgba(255,79,163,0.7)); transform: rotate(-8deg); }
  #jukebox-modal h2 {
    margin: 0; padding: 0; border: none; flex: none;
    font: italic 900 25px/1.1 "Arial Black", "Segoe UI Black", Impact, sans-serif;
    letter-spacing: 1.5px; word-spacing: 6px; text-transform: uppercase;
    background: linear-gradient(180deg, #fff6fb 0%, #ffc2e3 45%, #ff4fa3 100%);
    -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent;
    filter: drop-shadow(2px 2px 0 #2b0f3a) drop-shadow(0 0 10px rgba(255,79,163,0.55));
    animation: none; text-shadow: none;
  }
  #jukebox-modal .jb-kata {
    flex: none; transform: skewX(-12deg); padding: 3px 9px 4px; border-radius: 7px;
    font: 800 12px/1 "Yu Gothic UI", "Meiryo", "Hiragino Sans", "Noto Sans JP", sans-serif; letter-spacing: 1px;
    color: #10081c; background: linear-gradient(90deg, var(--jbY), #ffb347); box-shadow: 0 0 10px rgba(255,216,74,0.45);
  }
  #jukebox-modal .jb-subtitle { display: none; }
  #jukebox-modal .jb-top .jb-grow { flex: 1; }
  #jukebox-modal .jb-found {
    flex: none; padding: 5px 11px; border-radius: 6px; background: #040406; border: 1px solid #2a2833; border-top-color: #0a090d;
    font: 700 12px/1 ui-monospace, Consolas, monospace; color: var(--jbB); letter-spacing: 1.5px; text-shadow: 0 0 6px rgba(141,255,90,0.7);
  }
  /* a rubber key: a lighter top edge, a deep dark bottom edge - borders, so it keeps its shape with shadows off */
  #jukebox-modal .jb-x {
    flex: none; width: 34px; height: 34px; border-radius: 10px; cursor: pointer; box-sizing: border-box;
    border: 1px solid #0a090d; border-top-color: #4a4757; border-bottom: 4px solid #050407;
    background: linear-gradient(180deg, #34323d 0%, #1f1e25 100%); color: #d9d4e8; font: 900 15px/1 system-ui, sans-serif;
  }
  #jukebox-modal .jb-x:hover { color: #fff; outline: 2px solid rgba(255,79,163,0.8); outline-offset: 2px; }
  #jukebox-modal .jb-x:active { transform: translateY(2px); border-bottom-width: 2px; }
  /* ---- the deck module: a recessed plate holding deck A | meter + screen + transport | master volume ---- */
  #jukebox-modal .jb-deck { display: grid; grid-template-columns: 150px 1fr 150px; align-items: center; gap: 16px; padding: 12px 18px 10px; margin: 6px 16px 10px;
    border-radius: 18px; background: linear-gradient(180deg, #0c0b10 0%, #121117 30%, #141319 100%);
    border: 1px solid #050407; border-bottom-color: #2c2a35;
    box-shadow: inset 0 2px 10px rgba(0,0,0,0.85); }
  #jukebox-modal .jb-deckcol { display: flex; flex-direction: column; align-items: center; gap: 7px; }
  #jukebox-modal .jb-wheel { position: relative; width: 142px; height: 142px; margin: 0 auto; border-radius: 50%; padding: 7px; box-sizing: border-box;
    background: conic-gradient(from 200deg, var(--jbD), var(--jbY), var(--jbB), var(--jbA), var(--jbC), var(--jbD));
    outline: 3px solid #08070b; outline-offset: 0;
    box-shadow: 0 0 20px rgba(185,140,255,0.45); }
  /* the platter: a brushed chrome rim, then the grooves of the record (painted in the background, so it survives the
     low-graphics mode) */
  #jukebox-modal .jb-platter { position: relative; width: 100%; height: 100%; border-radius: 50%;
    background: radial-gradient(circle closest-side, rgba(0,0,0,0) 85%, #08070b 85.5% 87%, #3b3946 87.5%, #aeabbb 90%, #6e6b7b 94%, #08070b 95%),
                repeating-radial-gradient(circle, #121016 0 2px, #1c1a23 2px 4px);
    animation: jb-spin 1.9s linear infinite; animation-play-state: paused; }
  #jukebox-modal.jb-live .jb-platter { animation-play-state: running; }
  #jukebox-modal .jb-platter::before { content: ''; position: absolute; left: 50%; top: 13px; width: 5px; height: 12px; margin-left: -2.5px; border-radius: 3px; background: #fff; box-shadow: 0 0 6px #fff; }
  #jukebox-modal .jb-label { position: absolute; left: 50%; top: 50%; width: 48%; height: 48%; transform: translate(-50%, -50%); border-radius: 50%;
    box-sizing: border-box; border: 3px solid #07060b;
    background: radial-gradient(circle at 35% 30%, #fff 0%, #ffd6ec 40%, #ff8fca 100%); box-shadow: 0 0 12px rgba(255,79,163,0.6);
    display: flex; align-items: center; justify-content: center; overflow: hidden; }
  #jukebox-modal .jb-label img { width: 82%; height: 82%; object-fit: contain; }
  @keyframes jb-spin { to { transform: rotate(360deg); } }
