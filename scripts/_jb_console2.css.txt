  /* the master volume: a knurled aluminium knob in an LED arc that fills with the music volume; drag, scroll, arrow
     keys or the -/+ keys turn it */
  #jukebox-modal .jb-vol { --vol: 90; cursor: ns-resize; touch-action: none; outline: none;
    background: conic-gradient(from 225deg, var(--jbY) 0, var(--jbD) calc(var(--vol) * 0.75%), #24222b calc(var(--vol) * 0.75%), #24222b 75%, #0c0b10 75%); }
  #jukebox-modal .jb-vol:focus-visible { outline: 3px solid #fff; outline-offset: 2px; }
  #jukebox-modal .jb-knob { position: relative; width: 100%; height: 100%; border-radius: 50%; box-sizing: border-box;
    background: radial-gradient(circle at 50% 50%, #2a2831 0 54%, rgba(0,0,0,0) 55%),
                repeating-conic-gradient(from 0deg, #4a4756 0deg 4deg, #25232c 4deg 8deg);
    border: 3px solid #08070b; box-shadow: inset 0 10px 14px rgba(255,255,255,0.10), inset 0 -8px 12px rgba(0,0,0,0.5);
    display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 2px; }
  #jukebox-modal .jb-knob b { font: 900 26px/1 "Arial Black", "Segoe UI Black", sans-serif; color: #fff; text-shadow: 0 0 10px rgba(255,216,74,0.55); }
  #jukebox-modal .jb-knob small { font: 800 10px/1 ui-monospace, Consolas, monospace; letter-spacing: 2px; color: var(--jbY); }
  #jukebox-modal .jb-volbtns { display: flex; justify-content: center; gap: 36px; }
  /* rubber keys */
  #jukebox-modal .jb-volbtns button { width: 36px; height: 26px; box-sizing: border-box; border-radius: 7px; cursor: pointer; color: #d9d4e8; font: 900 15px/1 system-ui, sans-serif;
    border: 1px solid #0a090d; border-top-color: #4a4757; border-bottom: 4px solid #050407; background: linear-gradient(180deg, #34323d 0%, #1f1e25 100%); }
  #jukebox-modal .jb-volbtns button:hover { color: #fff; outline: 2px solid rgba(255,216,74,0.7); outline-offset: 2px; }
  #jukebox-modal .jb-volbtns button:active { transform: translateY(2px); border-bottom-width: 2px; }
  /* the middle: rainbow fan meter, the LCD, the transport */
  #jukebox-modal .jb-mid { display: flex; flex-direction: column; align-items: stretch; gap: 7px; min-width: 0; }
  #jukebox-modal .jb-fan { position: relative; height: 70px; margin: 0 auto; width: 250px; }
  #jukebox-modal .jb-fan i { position: absolute; left: 50%; bottom: 0; width: 8px; margin-left: -4px; height: 66px; transform-origin: 50% 100%; }
  #jukebox-modal .jb-fan i::before { content: ''; position: absolute; left: 0; right: 0; top: 0; height: 28px; border-radius: 4px; background: #1e1c25; border: 1px solid #08070b; box-sizing: border-box; }
  #jukebox-modal .jb-fan i::after { content: ''; position: absolute; left: 0; right: 0; top: 0; height: 28px; border-radius: 4px; background: var(--c); box-shadow: 0 0 8px var(--c);
    opacity: 0.35; transform-origin: 50% 100%; transform: scaleY(0.45); transition: opacity .2s; }
  #jukebox-modal.jb-live .jb-fan i::after { opacity: 1; animation: jb-fan 0.9s ease-in-out infinite alternate; animation-delay: var(--d); }
  @keyframes jb-fan { 0% { transform: scaleY(0.35); } 100% { transform: scaleY(1); } }
  /* the LCD: a dark glass screen with a faint pixel grid */
  #jukebox-now {
    display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 10px;
    padding: 8px 12px; border-radius: 8px; border: 1px solid #1c2b36; outline: 3px solid #0a090d; outline-offset: 0;
    background: repeating-linear-gradient(0deg, rgba(62,232,255,0.035) 0 1px, rgba(0,0,0,0) 1px 3px),
                repeating-linear-gradient(90deg, rgba(62,232,255,0.03) 0 1px, rgba(0,0,0,0) 1px 3px),
                linear-gradient(180deg, #050a10 0%, #030509 100%);
    box-shadow: inset 0 0 18px rgba(62,232,255,0.10), inset 0 2px 10px rgba(0,0,0,0.9);
  }
  #jukebox-now .jb-eq { display: flex; gap: 2px; align-items: flex-end; height: 18px; }
  #jukebox-now .jb-eq span { display: inline-block; width: 3px; height: 100%; border-radius: 1px; background: linear-gradient(180deg, var(--jbD), var(--jbA)); transform-origin: 50% 100%; animation: jb-eq 0.8s ease-in-out infinite; }
  #jukebox-now.idle .jb-eq span { animation-play-state: paused; opacity: 0.25; }
  #jukebox-now .jb-eq span:nth-child(2) { animation-delay: .12s; } #jukebox-now .jb-eq span:nth-child(3) { animation-delay: .24s; }
  #jukebox-now .jb-eq span:nth-child(4) { animation-delay: .36s; } #jukebox-now .jb-eq span:nth-child(5) { animation-delay: .48s; }
  @keyframes jb-eq { 0%, 100% { transform: scaleY(1); } 50% { transform: scaleY(0.3); } }
  #jukebox-now .jb-now-title { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
    font: 800 14px/1.2 ui-monospace, Consolas, "Segoe UI", monospace; letter-spacing: 0.5px; color: #bff6ff; text-shadow: 0 0 8px rgba(62,232,255,0.75); }
  #jukebox-now.idle .jb-now-title { color: #4f6a78; text-shadow: none; font-weight: 600; }
  #jukebox-now .jb-time { font: 700 13px/1 ui-monospace, Consolas, monospace; color: var(--jbA); letter-spacing: 1px; text-shadow: 0 0 6px rgba(62,232,255,0.7); }
  /* the transport: round rubber buttons in LED rings, their names printed under them */
  #jukebox-modal .jb-btns { display: flex; justify-content: center; gap: 26px; }
  #jukebox-modal .jb-tb { display: flex; flex-direction: column; align-items: center; gap: 6px; }
  #jukebox-modal .jb-btns button {
    --ring: 255,79,163; --lit: 0.45;
    width: 52px; height: 52px; padding: 0; box-sizing: border-box; border: 3px solid #08070b; border-radius: 50%; cursor: pointer;
    outline: 3px solid rgba(var(--ring), var(--lit)); outline-offset: 1px;
    display: inline-flex; align-items: center; justify-content: center; color: rgba(var(--ring), 1);
    background: radial-gradient(circle at 50% 35%, #3a3843 0%, #22212a 60%, #18171d 100%);
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.16), inset 0 -3px 5px rgba(0,0,0,0.55), 0 0 16px rgba(var(--ring), calc(var(--lit) * 0.8));
  }
  #jukebox-modal .jb-btns button svg { width: 18px; height: 18px; fill: currentColor; filter: drop-shadow(0 0 4px rgba(var(--ring), 0.8)); }
  #jukebox-modal .jb-btns button:hover { --lit: 1; }
  #jukebox-modal .jb-btns button:active { transform: translateY(3px); }
  #jukebox-modal .jb-btns #jukebox-stop { --ring: 255,79,163; }
  #jukebox-modal.jb-live .jb-btns #jukebox-stop { --lit: 1; }
  #jukebox-modal .jb-btns .jb-shuffle { --ring: 255,216,74; --lit: 0.8; }
  #jukebox-modal .jb-btns .jb-shuffle:hover { --lit: 1; }
