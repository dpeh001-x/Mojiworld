{
  "_comment": "Set 'relay' to your hosted wss:// relay URL before packaging (or export MOJI_RELAY_URL at build time). Empty = multiplayer disabled, solo play unaffected. See STEAM.md.",
  "relay": ""
}
